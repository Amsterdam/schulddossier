{% import 'partial.macros.html.twig' as tools %}
{% set dossierDocumenten = dossier.getNietVerwijderdeDocumentenByOnderwerp('voorlopigeTeruggaafBelastingdienst') %}
  
            <div class="accordion {{ app.request.isXmlHttpRequest ? 'active' }}" id="voorlegger-voorlopigeTeruggaafBelastingdienst">
                <div class="accordion-header">
                    
                    <div class="voorlegger-header{{ dossier.voorlegger.voorlopigeTeruggaafBelastingdienstNvt ? ' nvt' }}">
                        <h3 class="naam">
                            <a href="#voorlegger-voorlopigeTeruggaafBelastingdienst" data-handler="toggle">Voorlopige teruggaaf Belastingdienst</a>
                        </h3>
                        {% if dossierDocumenten|length > 0 %}
                          <div class="voortgang">
                            {{ dossierDocumenten|length }} document{% if dossierDocumenten|length != 1 %}en{% endif %}
                          </div>
                        {% endif %}
                        <div class="nvt">
                            {{ form_row(form.voorlopigeTeruggaafBelastingdienstNvt) }}
                        </div>
                        <div class="status-container">
                          {{ form_row(form.voorlopigeTeruggaafBelastingdienstOntvangenMadi) }}
                          {{ form_row(form.voorlopigeTeruggaafBelastingdienstOntvangenGka) }}
                        </div>
                    </div>
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file, 'defaultDocumentNaam': 'Voorlopige teruggaaf Belastingdienst'} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossierDocumenten, 'removeFile': form.removeFile} only %}
                        {{ form_row(form.aantekening) }}
                        {% include 'Dossier/partial.aantekeningen.html.twig' with {'aantekeningen': dossier.getAantekeningenByOnderwerp('voorlopigeTeruggaafBelastingdienst')} only %}
                    </div>
                </div>
            </div>
