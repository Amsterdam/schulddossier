{% import 'partial.macros.html.twig' as tools %}
{% set dossierDocumenten = dossier.getNietVerwijderdeDocumentenByOnderwerp('autolastenKmWoonwerkverkeer') %}
  
            <div class="accordion {{ app.request.isXmlHttpRequest ? 'active' }}" id="voorlegger-autolastenKmWoonwerkverkeer">
                <div class="accordion-header">
                    
                    <div class="voorlegger-header{{ dossier.voorlegger.autolastenKmWoonwerkverkeerNvt ? ' nvt' }}">
                        <h3 class="naam">
                            <a href="#voorlegger-autolastenKmWoonwerkverkeer" data-handler="toggle">Autolasten km woon-werk-verkeer</a>
                        </h3>
                        {% if dossierDocumenten|length > 0 %}
                          <div class="voortgang">
                            {{ dossierDocumenten|length }} document{% if dossierDocumenten|length != 1 %}en{% endif %}
                          </div>
                        {% endif %}
                        <div class="nvt">
                            {{ form_row(form.autolastenKmWoonwerkverkeerNvt) }}
                        </div>
                        <div class="status-container">
                          {{ form_row(form.autolastenKmWoonwerkverkeerOntvangenMadi) }}
                          {{ form_row(form.autolastenKmWoonwerkverkeerOntvangenGka) }}
                        </div>
                    </div>
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {{ form_row(form.autolastenKmWoonwerkverkeer) }}
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file, 'defaultDocumentNaam': 'Autolasten km woon-werk-verkeer'} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossierDocumenten, 'removeFile': form.removeFile} only %}
                        {{ form_row(form.aantekening) }}
                        {% include 'Dossier/partial.aantekeningen.html.twig' with {'aantekeningen': dossier.getAantekeningenByOnderwerp('autolastenKmWoonwerkverkeer')} only %}
                    </div>
                </div>
            </div>
