{% import 'partial.macros.html.twig' as tools %}
            {{ form_start(form) }}
            <div class="accordion {{ app.request.isXmlHttpRequest ? 'active' }}" id="voorlegger-stabilisatieovereenkomst">
                <div class="accordion-header">
                    <a href="#voorlegger-stabilisatieovereenkomst" data-handler="toggle">
                        <div class="voorlegger-header">
                            <div class="naam">
                                Stabilisatieovereenkomst
                            </div>
                            <div class="status">
                                <span class="{{ tools.madi_status_to_class(dossier.voorlegger.stabilisatieovereenkomstOntvangenMadi) }}">MaDi</span>
                                <span class="{{ tools.gka_status_to_class(dossier.voorlegger.stabilisatieovereenkomstOntvangenGka) }}">GKA</span>
                            </div>
                            <div class="nvt">
                                <span class="{{ dossier.voorlegger.stabilisatieovereenkomstNvt ? 'nvt' : 'open' }}">{{ dossier.voorlegger.stabilisatieovereenkomstNvt ? 'n.v.t.' }}</span>
                            </div>
                        </div>
                    </a>
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {{ form_row(form.stabilisatieovereenkomstNvt) }}
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file, 'defaultDocumentNaam': 'Stabilisatieovereenkomst'} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossier.getDocumentenByOnderwerp('stabilisatieovereenkomst')} only %}
                        {{ form_row(form.stabilisatieovereenkomstOntvangenMadi) }}
                        {{ form_row(form.stabilisatieovereenkomstOntvangenGka) }}
                        <p class="buttons right">
                            <button class="button primary" type="submit">Opslaan</button>
                        </p>
                    </div>
                </div>
            </div>
            {{ form_end(form) }}
