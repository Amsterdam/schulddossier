{% import 'partial.macros.html.twig' as tools %}
{% set dossierDocumenten = dossier.getDocumentenByOnderwerp('stabilisatieovereenkomst') %}
  
            <div class="accordion {{ app.request.isXmlHttpRequest ? 'active' }}" id="voorlegger-stabilisatieovereenkomst">
                <div class="accordion-header">
                    
                    <div class="voorlegger-header">
                        <h3 class="naam">
                            <a href="#voorlegger-stabilisatieovereenkomst" data-handler="toggle">Stabilisatieovereenkomst</a>
                        </h3>
                        {% if dossierDocumenten %}
                          <div class="voortgang">
                            {{ dossierDocumenten|length }} document{% if dossierDocumenten|length != 1 %}en{% endif %}
                          </div>
                        {% endif %}
                        <div class="status-container">
                          {{ form_row(form.stabilisatieovereenkomstOntvangenMadi) }}
                          {{ form_row(form.stabilisatieovereenkomstOntvangenGka) }}
                        </div>
                        <div class="nvt">
                            <span class="{{ dossier.voorlegger.stabilisatieovereenkomstNvt ? 'nvt' : 'open' }}">{{ dossier.voorlegger.stabilisatieovereenkomstNvt ? 'n.v.t.' }}</span>
                        </div>
                    </div>
                    
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {{ form_row(form.stabilisatieovereenkomstNvt) }}
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file, 'defaultDocumentNaam': 'Stabilisatieovereenkomst'} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossierDocumenten, 'removeFile': form.removeFile} only %}
                        {{ form_row(form.aantekening) }}
                        {% include 'Dossier/partial.aantekeningen.html.twig' with {'aantekeningen': dossier.getAantekeningenByOnderwerp('stabilisatieovereenkomst')} only %}
                    </div>
                </div>
            </div>
