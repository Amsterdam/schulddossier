
            {{ form_start(form, {'attr': {'class': 'ajax default-form-style'} }) }}
            <div class="accordion" id="voorlegger-stabilisatieovereenkomst">
                <div class="accordion-header">
                    <a href="#voorlegger-stabilisatieovereenkomst">
                        <div class="voorlegger-header">
                            <div class="naam">
                                Stabilisatieovereenkomst
                            </div>
                            <div class="status">
                                <span class="{{ dossier.voorlegger.stabilisatieovereenkomstOntvangenMadi ? 'afgerond' : 'open' }}">MaDi</span>
                                <span class="{{ dossier.voorlegger.stabilisatieovereenkomstOntvangenGka ? 'afgerond' : 'open' }}">GKA</span>
                            </div>
                            <div class="nvt">
                                <span class="{{ dossier.voorlegger.stabilisatieovereenkomstNvt ? 'nvt' : 'open' }}">{{ dossier.voorlegger.stabilisatieovereenkomstNvt ? 'n.v.t.' }}</span>
                            </div>
                        </div>
                    </a>
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {{ form_row(form.stabilisatieovereenkomstNvt) }}
                        {{ form_row(form.stabilisatieovereenkomstOntvangenMadi) }}
                        {{ form_row(form.stabilisatieovereenkomstOntvangenGka) }}
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossier.getDocumentenByOnderwerp('stabilisatieovereenkomst')} only %}
                        <div class="nav">
                            <button type="submit">Opslaan</button>
                        </div>
                    </div>
                </div>
            </div>
            {{ form_end(form) }}
