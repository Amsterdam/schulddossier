            {{ form_start(form, {'attr': {'class': 'ajax default-form-style'} }) }}
            <div class="accordion" id="voorlegger-alimentatieEchtscheidingsconvenant">
                <div class="accordion-header">
                    <div class="voorlegger-header">
                        <div class="naam">
                            <a href="#voorlegger-alimentatieEchtscheidingsconvenant">Alimentatie Echtscheidingsconvenant</a>
                        </div>
                        <div class="status">
                            <span class="{{ dossier.voorlegger.alimentatieEchtscheidingsconvenantOntvangenMadi ? 'afgerond' : 'open' }}">MaDi</span>
                            <span class="{{ dossier.voorlegger.alimentatieEchtscheidingsconvenantOntvangenGka ? 'afgerond' : 'open' }}">GKA</span>
                        </div>
                        <div class="nvt">
                            <span class="{{ dossier.voorlegger.alimentatieEchtscheidingsconvenantNvt ? 'nvt' : 'open' }}">{{ dossier.voorlegger.alimentatieEchtscheidingsconvenantNvt ? 'n.v.t.' }}</span>
                        </div>
                    </div>
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {{ form_row(form.alimentatieEchtscheidingsconvenantOntvangenMadi) }}
                        {{ form_row(form.alimentatieEchtscheidingsconvenantOntvangenGka) }}
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossier.getDocumentenByOnderwerp('alimentatieEchtscheidingsconvenant')} only %}
                        <div class="nav">
                            <button type="submit">Opslaan</button>
                        </div>
                    </div>
                </div>
            </div>
            {{ form_end(form) }}