{% import 'partial.macros.html.twig' as tools %}
{% set dossierDocumenten = dossier.getNietVerwijderdeDocumentenByOnderwerp('toeslagen') %}
  
            <div class="accordion {{ app.request.isXmlHttpRequest ? 'active' }}" id="voorlegger-toeslagen">
                <div class="accordion-header">
                    
                    <div class="voorlegger-header{{ dossier.voorlegger.toeslagenNvt ? ' nvt' }}">
                        <h3 class="naam">
                            <a href="#voorlegger-toeslagen" data-handler="toggle">Toeslagen</a>
                        </h3>
                        {% if dossierDocumenten|length > 0 %}
                          <div class="voortgang">
                            {{ dossierDocumenten|length }} document{% if dossierDocumenten|length != 1 %}en{% endif %}
                          </div>
                        {% endif %}
                        <div class="nvt" data-changer="nvt">
                            {{ form_row(form.toeslagenNvt) }}
                        </div>
                        <div class="status-container">
                          {{ form_row(form.toeslagenOntvangenMadi) }}
                          {{ form_row(form.toeslagenOntvangenGka) }}
                        </div>
                    </div>
                    
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {{ form_row(form.toeslagenHuur) }}
                        {{ form_row(form.toeslagenZorg) }}
                        {{ form_row(form.toeslagenKindgebondenBudget) }}
                        {{ form_row(form.toeslagenKinderopvang) }}
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file, 'defaultDocumentNaam': 'Huurtoeslag'} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossierDocumenten, 'removeFile': form.removeFile} only %}
                        {{ form_row(form.aantekening) }}
                        {% include 'Dossier/partial.aantekeningen.html.twig' with {'aantekeningen': dossier.getAantekeningenByOnderwerp('huurtoeslag')} only %}
                    </div>
                </div>
            </div>
