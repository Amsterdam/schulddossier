
            {{ form_start(form, {'attr': {'class': 'ajax default-form-style'} }) }}
            <div class="accordion" id="voorlegger-inkomstenspecificatie">
                <div class="accordion-header">
                    <a href="#voorlegger-inkomstenspecificatie">
                        <div class="voorlegger-header">
                            <div class="naam">
                                Inkomstenspecificatie
                            </div>
                            <div class="status">
                                <span class="{{ dossier.voorlegger.inkomstenspecificatieOntvangenMadi ? 'afgerond' : 'open' }}">MaDi</span>
                                <span class="{{ dossier.voorlegger.inkomstenspecificatieOntvangenGka ? 'afgerond' : 'open' }}">GKA</span>
                            </div>
                            <div class="nvt">
                            </div>
                        </div>
                    </a>
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {{ form_row(form.inkomstenspecificatieOntvangenMadi) }}
                        {{ form_row(form.inkomstenspecificatieOntvangenGka) }}
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossier.getDocumentenByOnderwerp('inkomstenspecificatie')} only %}
                        <div class="nav">
                            <button type="submit">Opslaan</button>
                        </div>
                    </div>
                </div>
            </div>
            {{ form_end(form) }}