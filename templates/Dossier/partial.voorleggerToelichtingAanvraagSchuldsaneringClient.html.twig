
            {{ form_start(form, {'attr': {'class': 'ajax default-form-style'} }) }}
            <div class="accordion" id="voorlegger-toelichtingAanvraagSchuldsaneringClient">
                <div class="accordion-header">
                    <a href="#voorlegger-toelichtingAanvraagSchuldsaneringClient">
                        <div class="voorlegger-header">
                            <div class="naam">
                                Toelichting aanvraag schuldsanering client
                            </div>
                            <div class="status">
                                <span class="{{ dossier.voorlegger.toelichtingAanvraagSchuldsaneringClientOntvangenMadi ? 'afgerond' : 'open' }}">MaDi</span>
                                <span class="{{ dossier.voorlegger.toelichtingAanvraagSchuldsaneringClientOntvangenGka ? 'afgerond' : 'open' }}">GKA</span>
                            </div>
                            <div class="nvt">
                            </div>
                        </div>
                    </a>
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {{ form_row(form.toelichtingAanvraagSchuldsaneringClientOntvangenMadi) }}
                        {{ form_row(form.toelichtingAanvraagSchuldsaneringClientOntvangenGka) }}
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossier.getDocumentenByOnderwerp('toelichtingAanvraagSchuldsaneringClient')} only %}
                        <div class="nav">
                            <button type="submit">Opslaan</button>
                        </div>
                    </div>
                </div>
            </div>
            {{ form_end(form) }}