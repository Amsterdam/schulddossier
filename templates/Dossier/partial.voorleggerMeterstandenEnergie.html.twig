{% import 'partial.macros.html.twig' as tools %}
            {{ form_start(form) }}
            <div class="accordion {{ app.request.isXmlHttpRequest ? 'active' }}" id="voorlegger-meterstandenEnergie">
                <div class="accordion-header">
                    <a href="#voorlegger-meterstandenEnergie" data-handler="toggle">
                        <div class="voorlegger-header">
                            <div class="naam">
                                Meterstanden energie
                            </div>
                            <div class="status">
                                <span class="{{ tools.madi_status_to_class(dossier.voorlegger.meterstandenEnergieOntvangenMadi) }}">MaDi</span>
                                <span class="{{ tools.gka_status_to_class(dossier.voorlegger.meterstandenEnergieOntvangenGka) }}">GKA</span>
                            </div>
                            <div class="nvt">
                                <span class="{{ dossier.voorlegger.meterstandenEnergieNvt ? 'nvt' : 'open' }}">{{ dossier.voorlegger.meterstandenEnergieNvt ? 'n.v.t.' }}</span>
                            </div>
                        </div>
                    </a>
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {{ form_row(form.meterstandenEnergieNvt) }}
                        {{ form_row(form.energieBedrijf) }}
                        {{ form_row(form.energieBedrijfKlantnummer) }}
                        {{ form_row(form.energieBedrijfDatumOpname) }}
                        {{ form_row(form.energieBedrijfT1) }}
                        {{ form_row(form.energieBedrijfT2) }}
                        {{ form_row(form.energieBedrijfGas) }}
                        {{ form_row(form.warmteBedrijf) }}
                        {{ form_row(form.warmteBedrijfKlantnummer) }}
                        {{ form_row(form.warmteBedrijfDatumOpname) }}
                        {{ form_row(form.warmteBedrijfOpname) }}
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file, 'defaultDocumentNaam': 'Meterstanden energie'} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossier.getDocumentenByOnderwerp('meterstandenEnergie')} only %}
                        {{ form_row(form.meterstandenEnergieOntvangenMadi) }}
                        {{ form_row(form.meterstandenEnergieOntvangenGka) }}
                        <p class="buttons right">
                            <button class="button primary" type="submit">Opslaan</button>
                        </p>
                    </div>
                </div>
            </div>
            {{ form_end(form) }}