<!DOCTYPE html>
<html lang="nl">
<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
<title>Dossierindex</title>
<meta name="viewport" content="width = device-width">
<link rel="stylesheet" href="/css/styles.css">
</head>

<body class="index">

	<header>
    <h1>Dossiers</h1>
    <p><a class="knop" href="{{ path('gemeenteamsterdam_fixxxschuldhulp_appdossier_create') }}">Maak nieuw dossier aan</a></p>
    </header>
    
    <h2>Bestaande dossiers</h2>
    <table>
        <thead>
            <tr>
                <th>Naam</th>
                <th>MaDi</th>
                <th>Regas</th>
                <th>Allegro</th>
            </tr>
        </thead>
        <tbody>
{% for dossier in dossiers %}
            <tr>
                <td><a href="{{ path('gemeenteamsterdam_fixxxschuldhulp_appdossier_detail', {'dossierId': dossier.Id}) }}">{{ dossier.clientNaam }}</a></td>
                <td>{{ dossier.schuldhulpbureau }}</td>
                <td class="nummer">
                {% if dossier.regasNummer is empty %}
                    -
                {% else %}    
                    {{ dossier.regasNummer }} 
                {% endif %}                
                </td>
                <td class="nummer">
                {% if dossier.allegroNummer is empty %}
                    -
                {% else %}    
                    {{ dossier.allegroNummer }} 
                {% endif %}
                </td>
            </tr>        
{% endfor %}
       </tbody>
    </table>
    
    
</body>
</html>