{% import 'partial.macros.html.twig' as tools %}
            <div class="accordion {{ app.request.isXmlHttpRequest ? 'active' }}" id="voorlegger-inzageToetsingBkr">
                <div class="accordion-header">
                    
                    <div class="voorlegger-header">
                        <h3 class="naam">
                            <a href="#voorlegger-inzageToetsingBkr" data-handler="toggle">Inzage toetsing BKR</a>
                        </h3>
                        <div class="status-container">
                          {{ form_row(form.inzageToetsingBkrOntvangenMadi) }}
                          {{ form_row(form.inzageToetsingBkrOntvangenGka) }}
                        </div>
                        <div class="nvt">
                        </div>
                    </div>
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file, 'defaultDocumentNaam': 'Inzage toetsing BKR'} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossier.getDocumentenByOnderwerp('inzageToetsingBkr'), 'removeFile': form.removeFile} only %}
                        {{ form_row(form.aantekening) }}
                        {% include 'Dossier/partial.aantekeningen.html.twig' with {'aantekeningen': dossier.getAantekeningenByOnderwerp('inzageToetsingBkr')} only %}
                    </div>
                </div>
            </div>
