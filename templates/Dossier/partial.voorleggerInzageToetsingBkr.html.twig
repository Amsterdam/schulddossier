
            {{ form_start(form, {'attr': {'class': 'ajax default-form-style'} }) }}
            <div class="accordion" id="voorlegger-inzageToetsingBkr">
                <div class="accordion-header">
                    <a href="#voorlegger-inzageToetsingBkr">
                        <div class="voorlegger-header">
                            <div class="naam">
                                Inzage toetsing BKR
                            </div>
                            <div class="status">
                                <span class="{{ dossier.voorlegger.inzageToetsingBkrOntvangenMadi ? 'afgerond' : 'open' }}">MaDi</span>
                                <span class="{{ dossier.voorlegger.inzageToetsingBkrOntvangenGka ? 'afgerond' : 'open' }}">GKA</span>
                            </div>
                            <div class="nvt">
                            </div>
                        </div>
                    </a>
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {{ form_row(form.inzageToetsingBkrOntvangenMadi) }}
                        {{ form_row(form.inzageToetsingBkrOntvangenGka) }}
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossier.getDocumentenByOnderwerp('inzageToetsingBkr')} only %}
                        <div class="nav">
                            <button type="submit">Opslaan</button>
                        </div>
                    </div>
                </div>
            </div>
            {{ form_end(form) }}