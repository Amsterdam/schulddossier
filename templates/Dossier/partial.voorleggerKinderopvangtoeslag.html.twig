            {% form_theme form with ['forms2.html.twig'] only %}
            {{ form_start(form, {'attr': {'class': 'ajax default-form-style'} }) }}
            <div class="accordion" id="voorlegger-kinderopvangtoeslag">
                <div class="accordion-header">
                    <div class="voorlegger-header">
                        <div class="naam">
                            <a href="#voorlegger-kinderopvangtoeslag">Kinderopvangtoeslag</a>
                        </div>
                        <div class="status">
                            <span class="{{ dossier.voorlegger.kinderopvangtoeslagOntvangenMadi ? 'afgerond' : 'open' }}">MaDi</span>
                            <span class="{{ dossier.voorlegger.kinderopvangtoeslagOntvangenGka ? 'afgerond' : 'open' }}">GKA</span>
                        </div>
                        <div class="nvt">
                            <span class="{{ dossier.voorlegger.kinderopvangtoeslagNvt ? 'nvt' : 'open' }}">{{ dossier.voorlegger.kinderopvangtoeslagNvt ? 'n.v.t.' }}</span>
                        </div>
                    </div>
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {{ form_row(form.kinderopvangtoeslagNvt) }}
                        {{ form_row(form.kinderopvangtoeslagOntvangenMadi) }}
                        {{ form_row(form.kinderopvangtoeslagOntvangenGka) }}
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossier.getDocumentenByOnderwerp('kinderopvangtoeslag')} only %}
                        <div class="nav">
                            <button type="submit">Opslaan</button>
                        </div>
                    </div>
                </div>
            </div>
            {{ form_end(form) }}