{% import 'partial.macros.html.twig' as tools %}
{% set dossierDocumenten = dossier.getNietVerwijderdeDocumentenByOnderwerp('kinderopvangtoeslag') %}
  
            <div class="accordion {{ app.request.isXmlHttpRequest ? 'active' }}" id="voorlegger-kinderopvangtoeslag">
                <div class="accordion-header">
                    
                    <div class="voorlegger-header{{ dossier.voorlegger.kinderopvangtoeslagNvt ? ' nvt' }}">
                        <h3 class="naam">
                            <a href="#voorlegger-kinderopvangtoeslag" data-handler="toggle">Kinderopvangtoeslag</a>
                        </h3>
                        {% if dossierDocumenten|length > 0 %}
                          <div class="voortgang">
                            {{ dossierDocumenten|length }} document{% if dossierDocumenten|length != 1 %}en{% endif %}
                          </div>
                        {% endif %}
                        <div class="nvt" data-changer="nvt">
                            {{ form_row(form.kinderopvangtoeslagNvt) }}
                        </div>
                        <div class="status-container">
                          {{ form_row(form.kinderopvangtoeslagOntvangenMadi) }}
                          {{ form_row(form.kinderopvangtoeslagOntvangenGka) }}
                        </div>
                    </div>
                </div>
                <div class="accordion-body">
                    <div class="voorlegger-body">
                        {% include 'partial.files-container.html.twig' with {'form_elm': form.file, 'defaultDocumentNaam': 'Kinderopvangtoeslag'} only %}
                        {% include 'partial.files-list.html.twig' with {'dossierDocumenten': dossierDocumenten, 'removeFile': form.removeFile} only %}
                        {{ form_row(form.aantekening) }}
                        {% include 'Dossier/partial.aantekeningen.html.twig' with {'aantekeningen': dossier.getAantekeningenByOnderwerp('kinderopvangtoeslag')} only %}
                    </div>
                </div>
            </div>
