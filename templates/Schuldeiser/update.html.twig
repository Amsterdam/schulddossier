{% extends 'master.html.twig' %}

{% block title %}Schuldeiser: {{ schuldeiser.bedrijfsnaam }} - {{ parent() }}{% endblock %}

{% block pdfsplitter %}{% endblock %}

{% block document %}

<p class="nav-internal--breadcrumb">
    <a href="{{ path('gemeenteamsterdam_fixxxschuldhulp_appdossier_index') }}" class=" nav-internal--breadcrumb__home">
        <span>Start</span>
    </a>
    <a href="{{ path('gemeenteamsterdam_fixxxschuldhulp_appschuldeiser_index') }}" class=" nav-internal--breadcrumb__dossier">
        <i class="nav-internal--breadcrumb__icon-arrow-right"></i>
        <span>Schuldeisers</span>
    </a>
    <a href="#sectie" class="nav-internal--breadcrumb__section in-active">
        <i class="nav-internal--breadcrumb__icon-arrow-right"></i>
        <span>Schuldeiser: {{ schuldeiser.bedrijfsnaam }}</span>
    </a>
</p>

<div class="document well">
    <div class="document-header">
        <h1 class="document-title">Schuldeiser: {{ schuldeiser.bedrijfsnaam }}</h1>
    </div>
    <div class="form-container">
        {{ form_start(form, {'attr': {'class': 'default-form-style'} }) }}
        {{ form_row(form.bedrijfsnaam) }}
        {% if app.user.type == 'admin' %}
            {{ form_help(form.bedrijfsnaam) }}
        {% endif %}
        {{ form_row(form.enabled) }}
        {% if app.user.type == 'admin' %}
            {{ form_help(form.enabled) }}
        {% endif %}
        {{ form_row(form.allegroCode) }}
        {% if app.user.type == 'admin' %}
            {{ form_help(form.allegroCode) }}
        {% endif %}
        {{ form_row(form.rekening) }}
        {% if app.user.type == 'admin' %}
            {{ form_help(form.rekening) }}
        {% endif %}
        {{ form_row(form.straat) }}
        {% if app.user.type == 'admin' %}
            {{ form_help(form.straat) }}
        {% endif %}
        {{ form_row(form.huisnummer) }}
        {% if app.user.type == 'admin' %}
            {{ form_help(form.huisnummer) }}
        {% endif %}
        {{ form_row(form.huisnummerToevoeging) }}
        {% if app.user.type == 'admin' %}
            {{ form_help(form.huisnummerToevoeging) }}
        {% endif %}
        {{ form_row(form.postcode) }}
        {% if app.user.type == 'admin' %}
            {{ form_help(form.postcode) }}
        {% endif %}
        {{ form_row(form.plaats) }}
        {% if app.user.type == 'admin' %}
            {{ form_help(form.plaats) }}
        {% endif %}
        {{ form_row(form.opmerkingen) }}
        {% if app.user.type == 'admin' %}
            {{ form_help(form.opmerkingen) }}
        {% endif %}
        <p class="buttons right">
            <button type="submit" class="button primary">Opslaan</button>
            <a href="{{ path('gemeenteamsterdam_fixxxschuldhulp_appschuldeiser_index') }}">Annuleren</a>
        </p>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
